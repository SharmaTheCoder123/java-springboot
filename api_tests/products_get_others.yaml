# ********RoostGPT********

# Test generated by RoostGPT for test API-Verify using AI Type  and AI Model 
# 
# Test generated for /products_get for http method type GET in artillery framework
# RoostTestHash=ed0d766b50
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields: ["id", "name"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10

scenarios:
  - name: "Test scenario for various non-success API requests"
    flow:
      - log: "Sending a GET request with invalid URL"
      - get:
          url: "/invalidURL"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404

      - think: 1

      - log: "Sending a GET request without Authorization header"
      - get:
          url: "/products"
          expect:
            - statusCode: 401

      - think: 1

      - log: "Sending a GET request with an invalid product ID"
      - get:
          url: "/products/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404

      - think: 1

      - log: "Sending a GET request with an invalid product name"
      - get:
          url: "/products/{{ name }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
